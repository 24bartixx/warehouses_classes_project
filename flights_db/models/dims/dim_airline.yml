version: 2

models:
  - name: dim_airline
    columns:
      - name: airline_id
        data_tests:
          - not_null
          - unique
      - name: iata_code
        data_tests:
          - not_null
          - unique
      - name: airline_name
        data_tests:
          - not_null
          - unique
